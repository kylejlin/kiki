use crate::cst::*;

grammar;

pub Term: i32 = {
    <n:Underscore> => 0,
    <n:Ident> => 1,
    "(" <t:Term> ")" => t,
};

pub Underscore: () = "_" => ();
pub Ident: Ident = <s:r"[a-zA-Z_][a-zA-Z_0-9]*"> => Ident {
    name: s.to_owned(),
    // TODO
    position: ByteIndex(0),
};
